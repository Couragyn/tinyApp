<!-- view for listing all URLs in dic -->
<!DOCTYPE html>
<html lang="en">
<head>
</head>
<body>
  <ol>
    <% include partials/__header %>
    <% for (prop in urls) { %>
    <form action='/urls/<%= prop %>/delete' method='post'>
        <li> <%= urls[prop] %> - <%= prop %>
          <a href="urls/<%= prop %>">edit</a>
            <button>Delete</button>
        </li>
    </form>

      <% }; %>
  </ol>
    <a href="./new">New shorten URL</a>
</body>
</html>
